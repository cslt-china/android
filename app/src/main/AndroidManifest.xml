<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zhichongh.collectsignlanguage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.data_score.DataListActivity" />
        <activity android:name=".ui.data_score.WatchVideoActivity" />
        <activity android:name=".ui.data_score.ScoreActivity" />
        <activity android:name=".ui.data_collection.CollectDataMenuActivity" />
        <activity android:name=".ui.user.RegisterActivity" />
        <activity android:name=".ui.user.LoginActivity" />
        <activity android:name=".ui.user.PersonCenterMenuActivity" />
        <activity android:name=".ui.user.StatisticTableActivity" />
        <activity android:name=".ui.user.ScoreRecordActivity" />
        <activity android:name=".ui.user.CollectRecordActivity"></activity>
    </application>

</manifest>