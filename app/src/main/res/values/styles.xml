<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/highLight</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="Spacer">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dip</item>
        <item name="android:background">#FF909090</item>
    </style>

    <style name="label_list_title">
        <item name="android:layout_marginTop">14dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TaskCard">
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_margin">@dimen/fab_margin</item>
        <item name="android:elevation">3dp</item>
        <item name="contentPadding">10dp</item>
        <item name="cardCornerRadius">10dp</item>
    </style>
    <style name="TaskCard.Prompt">
        <item name="cardBackgroundColor">@color/light_grey</item>
    </style>
    <style name="TaskCard.Approved">
        <item name="cardBackgroundColor">@color/green</item>
    </style>
    <style name="TaskCard.PendingApproval">
        <item name="android:background">@color/yellow</item>
    </style>
    <style name="TaskCard.Rejected">
        <item name="android:background">@color/red</item>
    </style>
</resources>
